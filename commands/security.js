const {SlashCommandBuilder}=require("discord.js");const {N}=require("../core/identity/registry");const {getGuildConfig}=require("../core/config/store");const {state}=require("../core/containment/modes");module.exports={data:new SlashCommandBuilder().setName(N.commands.security.name).setDescription(N.commands.security.description),async execute(i){const c=getGuildConfig(i.guild.id),s=state(i.guild);await i.reply({ephemeral:true,content:`${N.product.name} security posture\nEnabled: ${c.security.enabled}\nPanic: ${s.panic}\nRaid: ${s.raid}\nAction: ${c.security.action}\nLog channel: ${c.security.logChannelId||"not configured"}`});}};