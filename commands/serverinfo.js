const {SlashCommandBuilder}=require("discord.js");const {N}=require("../core/identity/registry");module.exports={data:new SlashCommandBuilder().setName(N.commands.serverinfo.name).setDescription(N.commands.serverinfo.description),async execute(i){const g=i.guild;await i.reply({ephemeral:true,content:`${N.product.name} server telemetry\nServer: ${g.name}\nOwner: ${g.ownerId}\nMembers: ${g.memberCount}\nCreated: ${g.createdAt.toISOString()}\nChannels: ${g.channels.cache.size}\nRoles: ${g.roles.cache.size}`});}};