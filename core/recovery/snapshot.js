const snapshots=new Map();function snapshotGuild(guild){const s={time:Date.now(),guildId:guild.id,roles:guild.roles.cache.map(r=>({id:r.id,name:r.name,permissions:r.permissions.bitfield.toString(),position:r.position})),channels:guild.channels.cache.map(c=>({id:c.id,name:c.name,type:c.type,parentId:c.parentId}))};snapshots.set(guild.id,s);return s;}function getSnapshot(id){return snapshots.get(id)||null;}module.exports={snapshotGuild,getSnapshot};