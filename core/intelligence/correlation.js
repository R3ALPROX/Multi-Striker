const { recent }=require("./memory");function correlate(guildId,windowMs=120000){const es=recent(guildId,windowMs).filter(e=>e.securityEvent);const m=new Map();for(const e of es){if(!e.executorId)continue;const a=m.get(e.executorId)||{executorId:e.executorId,count:0,types:new Set()};a.count++;a.types.add(e.actionType);m.set(e.executorId,a);}const actors=[...m.values()].filter(a=>a.count>=2);return{coordinated:actors.length>=2&&actors.reduce((n,a)=>n+a.count,0)>=5,actors:actors.map(a=>({executorId:a.executorId,count:a.count,types:[...a.types]})),eventCount:es.length};}module.exports={correlate};