const {N}=require("../identity/registry");const {sendLog,incidentEmbed}=require("../observability/logger");const {setPanic}=require("../containment/modes");const {forwardIncident}=require("../network/reporter");const cooldown=new Map();
async function alert(guild,incident){recordIncident(guild,incident.severity);const key=`${guild.id}:${incident.type}`,now=Date.now(),last=cooldown.get(key)||0;const logPromise=sendLog(guild,incident).catch(()=>false);const networkPromise=forwardIncident({...incident,guildId:guild.id,guildName:guild.name}).catch(()=>false);if(now-last<30000){await Promise.allSettled([logPromise,networkPromise]);return;}cooldown.set(key,now);const ownerPromise=guild.fetchOwner().catch(()=>null);const [,owner]=await Promise.all([Promise.allSettled([logPromise,networkPromise]),ownerPromise]);if(owner)await owner.send({embeds:[incidentEmbed(incident)]}).catch(()=>{});}
async function critical(guild,data){setPanic(guild,true,data.description||"critical security signal");return alert(guild,{...data,severity:N.labels.critical});}module.exports={alert,critical};