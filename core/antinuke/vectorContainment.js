const { containMember }=require("./actions");const { quarantineMember }=require("../quarantine/manager");const { triggerPanic }=require("../panic/manager");async function containVector(guild,executorId,reason,meta={}){const containment=await containMember(guild,executorId,reason);const quarantine=await quarantineMember(guild,executorId,reason).catch(e=>({ok:false,message:e.message}));if(meta.critical)await triggerPanic(guild,reason,{executorId,...meta});return{containment,quarantine};}module.exports={containVector};